1. 文档收集与解析
- 结构化解析： LayoutPDFReader，Layout-parser ，针对不同的文档类型不同的设计
PubLayNet、DocLayNet
https://python.langchain.com/docs/integrations/document_loaders/llmsherpa/

2. 文档切分策略
 综合应用结构化切分和内容感知切分，可以有效控制知识块粒度，同时保留章节标题等信息作为元数据
🧠 LLM识别标题	用 LLM 识别文档结构如“章节/标题/正文”，用于更智能分割
📖 按格式分割	针对 Markdown、PDF 页面、HTML 结构进行格式驱动的分割
🔀 分割策略融合	中文按“句号/段落”，英文按“标点/换行”混合策略更精细
🧩 嵌入前预清洗	在分割前剔除页脚、目录页、模板页等噪声内容

3.向量化嵌入生成
嵌入生成流程： 对每个文档块调用嵌入模型，得到高维语义向量。
建议先清洗文本（去除噪声、统一编码），并保存相关元数据（标题、来源文件名等）。生成的嵌入用于构建向量索引，以便后续检索和分类。

4. 向量数据库构建
Chroma则支持通过Python API直接添加向量及其元数据

5. 自动主题分类
嵌入聚类： 利用向量表示可对文档进行无监督聚类以发现主题。常见方法是对文档块的嵌入进行K-means、HDBSCAN等算法聚类，将相似文档块归为同一主题簇。Vertex AI文档指出，对文本进行聚类可以通过优化的嵌入空间将语义相近的文本分组
cloud.google.com
。实际中，可先用PCA或UMAP降维，再聚类，可提高效果。每个簇可通过最具代表性的文档块自动生成主题摘要，作为标签。
LLM分类： 也可以使用大模型进行零样本主题分类。通过预先定义类别列表，让LLM对文档摘要或块内容做“请将该文档归为哪类主题”的推理
TfidfVectorizer： 关键词提取
。例如，用ChatGLM等生成器询问“这段文本的主题是什么？”。LlamaIndex建议，可先索引文本，提取语义特征，再让LLM根据内容生成分类标签
milvus.io
。这种方法无需人工标注样本，但精度依赖提示设计。


TOOD: 无法使用
- https://github.com/datalab-to/marker pdf测试

## 模型路径
- /root/.cache/datalab/models/text_recognition/
## 依赖模型
- text_recognition

