{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: pytest",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/tests/tasks/task_factory_multimodal.py",  // 指定测试目录
            "args": ["-v"],  // 可选，显示详细的测试输出
            "console": "integratedTerminal",
            "justMyCode": false,
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    ]
}
